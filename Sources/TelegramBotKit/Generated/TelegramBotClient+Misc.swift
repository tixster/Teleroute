// Generated by Scripts/generate-client.py — do not edit.
// Regenerate with Scripts/generate-api.sh.

import Foundation
import OpenAPIRuntime
import TelegramBotAPI

public extension TelegramBotClient {
    /// Use this method to get a list of profile pictures for a user. Returns a UserProfilePhotos
    /// object.
    @discardableResult
    func getUserProfilePhotos(userId: Swift.Int64, offset: Swift.Int64? = nil, limit: Swift.Int64? = nil) async throws -> Components.Schemas.UserProfilePhotos {
        let output = try await self.api.getUserProfilePhotos(.init(body: .json(.init(
            userId: userId,
            offset: offset,
            limit: limit
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "getUserProfilePhotos", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to get a list of profile audios for a user. Returns a UserProfileAudios object.
    @discardableResult
    func getUserProfileAudios(userId: Swift.Int64, offset: Swift.Int64? = nil, limit: Swift.Int64? = nil) async throws -> Components.Schemas.UserProfileAudios {
        let output = try await self.api.getUserProfileAudios(.init(body: .json(.init(
            userId: userId,
            offset: offset,
            limit: limit
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "getUserProfileAudios", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Changes the emoji status for a given user that previously allowed the bot to manage their emoji
    /// status via the Mini App method requestEmojiStatusAccess. Returns True on success.
    @discardableResult
    func setUserEmojiStatus(userId: Swift.Int64, emojiStatusCustomEmojiId: Swift.String? = nil, emojiStatusExpirationDate: Swift.Int64? = nil) async throws -> Swift.Bool {
        let output = try await self.api.setUserEmojiStatus(.init(body: .json(.init(
            userId: userId,
            emojiStatusCustomEmojiId: emojiStatusCustomEmojiId,
            emojiStatusExpirationDate: emojiStatusExpirationDate
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "setUserEmojiStatus", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to get basic information about a file and prepare it for downloading. For the
    /// moment, bots can download files of up to 20MB in size. On success, a File object is returned.
    /// The file can then be downloaded via the link
    /// https://api.telegram.org/file/bot<token>/<file_path>, where <file_path> is taken from the
    /// response. It is guaranteed that the link will be valid for at least 1 hour. When the link
    /// expires, a new one can be requested by calling getFile again. Note: This function ma...
    @discardableResult
    func getFile(fileId: Swift.String) async throws -> Components.Schemas.File {
        let output = try await self.api.getFile(.init(body: .json(.init(
            fileId: fileId
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "getFile", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to send answers to callback queries sent from inline keyboards. The answer will
    /// be displayed to the user as a notification at the top of the chat screen or as an alert. On
    /// success, True is returned. Alternatively, the user can be redirected to the specified Game URL.
    /// For this option to work, you must first create a game for your bot via @BotFather and accept the
    /// terms. Otherwise, you may use links like t.me/your_bot?start=XXXX that open your bot with a
    /// parameter.
    @discardableResult
    func answerCallbackQuery(callbackQueryId: Swift.String, text: Swift.String? = nil, showAlert: Swift.Bool? = nil, url: Swift.String? = nil, cacheTime: Swift.Int64? = nil) async throws -> Swift.Bool {
        let output = try await self.api.answerCallbackQuery(.init(body: .json(.init(
            callbackQueryId: callbackQueryId,
            text: text,
            showAlert: showAlert,
            url: url,
            cacheTime: cacheTime
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "answerCallbackQuery", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to get information about the connection of the bot with a business account.
    /// Returns a BusinessConnection object on success.
    @discardableResult
    func getBusinessConnection(businessConnectionId: Swift.String) async throws -> Components.Schemas.BusinessConnection {
        let output = try await self.api.getBusinessConnection(.init(body: .json(.init(
            businessConnectionId: businessConnectionId
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "getBusinessConnection", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to get the token of a managed bot. Returns the token as String on success.
    @discardableResult
    func getManagedBotToken(userId: Swift.Int64) async throws -> Swift.String {
        let output = try await self.api.getManagedBotToken(.init(body: .json(.init(
            userId: userId
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "getManagedBotToken", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to revoke the current token of a managed bot and generate a new one. Returns the
    /// new token as String on success.
    @discardableResult
    func replaceManagedBotToken(userId: Swift.Int64) async throws -> Swift.String {
        let output = try await self.api.replaceManagedBotToken(.init(body: .json(.init(
            userId: userId
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "replaceManagedBotToken", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to get the access settings of a managed bot. Returns a BotAccessSettings object
    /// on success.
    @discardableResult
    func getManagedBotAccessSettings(userId: Swift.Int64) async throws -> Components.Schemas.BotAccessSettings {
        let output = try await self.api.getManagedBotAccessSettings(.init(body: .json(.init(
            userId: userId
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "getManagedBotAccessSettings", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to change the access settings of a managed bot. Returns True on success.
    @discardableResult
    func setManagedBotAccessSettings(userId: Swift.Int64, isAccessRestricted: Swift.Bool, addedUserIds: [Swift.Int64]? = nil) async throws -> Swift.Bool {
        let output = try await self.api.setManagedBotAccessSettings(.init(body: .json(.init(
            userId: userId,
            isAccessRestricted: isAccessRestricted,
            addedUserIds: addedUserIds
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "setManagedBotAccessSettings", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to change the list of the bot's commands. See this manual for more details about
    /// bot commands. Returns True on success.
    @discardableResult
    func setMyCommands(commands: [Components.Schemas.BotCommand], scope: Components.Schemas.BotCommandScope? = nil, languageCode: Swift.String? = nil) async throws -> Swift.Bool {
        let output = try await self.api.setMyCommands(.init(body: .json(.init(
            commands: commands,
            scope: scope,
            languageCode: languageCode
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "setMyCommands", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to get the current list of the bot's commands for the given scope and user
    /// language. Returns an Array of BotCommand objects. If commands aren't set, an empty list is
    /// returned.
    @discardableResult
    func getMyCommands(scope: Components.Schemas.BotCommandScope? = nil, languageCode: Swift.String? = nil) async throws -> [Components.Schemas.BotCommand] {
        let output = try await self.api.getMyCommands(.init(body: .json(.init(
            scope: scope,
            languageCode: languageCode
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "getMyCommands", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to change the bot's name. Returns True on success.
    @discardableResult
    func setMyName(name: Swift.String? = nil, languageCode: Swift.String? = nil) async throws -> Swift.Bool {
        let output = try await self.api.setMyName(.init(body: .json(.init(
            name: name,
            languageCode: languageCode
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "setMyName", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to get the current bot name for the given user language. Returns BotName on
    /// success.
    @discardableResult
    func getMyName(languageCode: Swift.String? = nil) async throws -> Components.Schemas.BotName {
        let output = try await self.api.getMyName(.init(body: .json(.init(
            languageCode: languageCode
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "getMyName", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to change the bot's description, which is shown in the chat with the bot if the
    /// chat is empty. Returns True on success.
    @discardableResult
    func setMyDescription(description: Swift.String? = nil, languageCode: Swift.String? = nil) async throws -> Swift.Bool {
        let output = try await self.api.setMyDescription(.init(body: .json(.init(
            description: description,
            languageCode: languageCode
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "setMyDescription", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to get the current bot description for the given user language. Returns
    /// BotDescription on success.
    @discardableResult
    func getMyDescription(languageCode: Swift.String? = nil) async throws -> Components.Schemas.BotDescription {
        let output = try await self.api.getMyDescription(.init(body: .json(.init(
            languageCode: languageCode
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "getMyDescription", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to change the bot's short description, which is shown on the bot's profile page
    /// and is sent together with the link when users share the bot. Returns True on success.
    @discardableResult
    func setMyShortDescription(shortDescription: Swift.String? = nil, languageCode: Swift.String? = nil) async throws -> Swift.Bool {
        let output = try await self.api.setMyShortDescription(.init(body: .json(.init(
            shortDescription: shortDescription,
            languageCode: languageCode
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "setMyShortDescription", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to get the current bot short description for the given user language. Returns
    /// BotShortDescription on success.
    @discardableResult
    func getMyShortDescription(languageCode: Swift.String? = nil) async throws -> Components.Schemas.BotShortDescription {
        let output = try await self.api.getMyShortDescription(.init(body: .json(.init(
            languageCode: languageCode
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "getMyShortDescription", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Changes the profile photo of the bot. Returns True on success.
    @discardableResult
    func setMyProfilePhoto(photo: Components.Schemas.InputProfilePhoto) async throws -> Swift.Bool {
        var parts: [Operations.SetMyProfilePhoto.Input.Body.MultipartFormPayload] = []
        parts.append(.photo(.init(payload: .init(body: photo))))
        let output = try await self.api.setMyProfilePhoto(.init(body: .multipartForm(.init(parts))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "setMyProfilePhoto", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Removes the profile photo of the bot. Requires no parameters. Returns True on success.
    @discardableResult
    func removeMyProfilePhoto() async throws -> Swift.Bool {
        let output = try await self.api.removeMyProfilePhoto(.init())
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "removeMyProfilePhoto", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to change the default administrator rights requested by the bot when it's added
    /// as an administrator to groups or channels. These rights will be suggested to users, but they are
    /// free to modify the list before adding the bot. Returns True on success.
    @discardableResult
    func setMyDefaultAdministratorRights(rights: Components.Schemas.ChatAdministratorRights? = nil, forChannels: Swift.Bool? = nil) async throws -> Swift.Bool {
        let output = try await self.api.setMyDefaultAdministratorRights(.init(body: .json(.init(
            rights: rights,
            forChannels: forChannels
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "setMyDefaultAdministratorRights", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to get the current default administrator rights of the bot. Returns
    /// ChatAdministratorRights on success.
    @discardableResult
    func getMyDefaultAdministratorRights(forChannels: Swift.Bool? = nil) async throws -> Components.Schemas.ChatAdministratorRights {
        let output = try await self.api.getMyDefaultAdministratorRights(.init(body: .json(.init(
            forChannels: forChannels
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "getMyDefaultAdministratorRights", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Verifies a user on behalf of the organization which is represented by the bot. Returns True on
    /// success.
    @discardableResult
    func verifyUser(userId: Swift.Int64, customDescription: Swift.String? = nil) async throws -> Swift.Bool {
        let output = try await self.api.verifyUser(.init(body: .json(.init(
            userId: userId,
            customDescription: customDescription
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "verifyUser", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Removes verification from a user who is currently verified on behalf of the organization
    /// represented by the bot. Returns True on success.
    @discardableResult
    func removeUserVerification(userId: Swift.Int64) async throws -> Swift.Bool {
        let output = try await self.api.removeUserVerification(.init(body: .json(.init(
            userId: userId
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "removeUserVerification", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Changes the first and last name of a managed business account. Requires the can_change_name
    /// business bot right. Returns True on success.
    @discardableResult
    func setBusinessAccountName(businessConnectionId: Swift.String, firstName: Swift.String, lastName: Swift.String? = nil) async throws -> Swift.Bool {
        let output = try await self.api.setBusinessAccountName(.init(body: .json(.init(
            businessConnectionId: businessConnectionId,
            firstName: firstName,
            lastName: lastName
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "setBusinessAccountName", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Changes the username of a managed business account. Requires the can_change_username business
    /// bot right. Returns True on success.
    @discardableResult
    func setBusinessAccountUsername(businessConnectionId: Swift.String, username: Swift.String? = nil) async throws -> Swift.Bool {
        let output = try await self.api.setBusinessAccountUsername(.init(body: .json(.init(
            businessConnectionId: businessConnectionId,
            username: username
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "setBusinessAccountUsername", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Changes the bio of a managed business account. Requires the can_change_bio business bot right.
    /// Returns True on success.
    @discardableResult
    func setBusinessAccountBio(businessConnectionId: Swift.String, bio: Swift.String? = nil) async throws -> Swift.Bool {
        let output = try await self.api.setBusinessAccountBio(.init(body: .json(.init(
            businessConnectionId: businessConnectionId,
            bio: bio
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "setBusinessAccountBio", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Changes the profile photo of a managed business account. Requires the can_edit_profile_photo
    /// business bot right. Returns True on success.
    @discardableResult
    func setBusinessAccountProfilePhoto(businessConnectionId: Swift.String, photo: Components.Schemas.InputProfilePhoto, isPublic: Swift.Bool? = nil) async throws -> Swift.Bool {
        var parts: [Operations.SetBusinessAccountProfilePhoto.Input.Body.MultipartFormPayload] = []
        parts.append(.businessConnectionId(.init(payload: .init(body: HTTPBody(businessConnectionId)))))
        parts.append(.photo(.init(payload: .init(body: photo))))
        if let isPublic {
            parts.append(.isPublic(.init(payload: .init(body: HTTPBody(isPublic ? "true" : "false")))))
        }
        let output = try await self.api.setBusinessAccountProfilePhoto(.init(body: .multipartForm(.init(parts))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "setBusinessAccountProfilePhoto", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Removes the current profile photo of a managed business account. Requires the
    /// can_edit_profile_photo business bot right. Returns True on success.
    @discardableResult
    func removeBusinessAccountProfilePhoto(businessConnectionId: Swift.String, isPublic: Swift.Bool? = nil) async throws -> Swift.Bool {
        let output = try await self.api.removeBusinessAccountProfilePhoto(.init(body: .json(.init(
            businessConnectionId: businessConnectionId,
            isPublic: isPublic
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "removeBusinessAccountProfilePhoto", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Stores a keyboard button that can be used by a user within a Mini App. Returns a
    /// PreparedKeyboardButton object.
    @discardableResult
    func savePreparedKeyboardButton(userId: Swift.Int64, button: Components.Schemas.KeyboardButton) async throws -> Components.Schemas.PreparedKeyboardButton {
        let output = try await self.api.savePreparedKeyboardButton(.init(body: .json(.init(
            userId: userId,
            button: button
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "savePreparedKeyboardButton", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to approve a suggested post in a direct messages chat. The bot must have the
    /// 'can_post_messages' administrator right in the corresponding channel chat. Returns True on
    /// success.
    @discardableResult
    func approveSuggestedPost(chatId: Swift.Int64, messageId: Swift.Int64, sendDate: Swift.Int64? = nil) async throws -> Swift.Bool {
        try await self.pace(chatId: .id(chatId))
        let output = try await self.api.approveSuggestedPost(.init(body: .json(.init(
            chatId: chatId,
            messageId: messageId,
            sendDate: sendDate
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "approveSuggestedPost", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to decline a suggested post in a direct messages chat. The bot must have the
    /// 'can_manage_direct_messages' administrator right in the corresponding channel chat. Returns True
    /// on success.
    @discardableResult
    func declineSuggestedPost(chatId: Swift.Int64, messageId: Swift.Int64, comment: Swift.String? = nil) async throws -> Swift.Bool {
        try await self.pace(chatId: .id(chatId))
        let output = try await self.api.declineSuggestedPost(.init(body: .json(.init(
            chatId: chatId,
            messageId: messageId,
            comment: comment
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "declineSuggestedPost", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Informs a user that some of the Telegram Passport elements they provided contains errors. The
    /// user will not be able to re-submit their Passport to you until the errors are fixed (the
    /// contents of the field for which you returned the error must change). Returns True on success.
    /// Use this if the data submitted by the user doesn't satisfy the standards your service requires
    /// for any reason. For example, if a birthday date seems invalid, a submitted document is blurry, a
    /// scan shows evidence of tampe...
    @discardableResult
    func setPassportDataErrors(userId: Swift.Int64, errors: [Components.Schemas.PassportElementError]) async throws -> Swift.Bool {
        let output = try await self.api.setPassportDataErrors(.init(body: .json(.init(
            userId: userId,
            errors: errors
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "setPassportDataErrors", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to set the score of the specified user in a game message. On success, if the
    /// message is not an inline message, the Message is returned, otherwise True is returned. Returns
    /// an error, if the new score is not greater than the user's current score in the chat and force is
    /// False.
    @discardableResult
    func setGameScore(userId: Swift.Int64, score: Swift.Int64, force: Swift.Bool? = nil, disableEditMessage: Swift.Bool? = nil, chatId: Swift.Int64? = nil, messageId: Swift.Int64? = nil, inlineMessageId: Swift.String? = nil) async throws -> Components.Schemas.Message? {
        try await self.pace(chatId: chatId.map(ChatId.id))
        let output = try await self.api.setGameScore(.init(body: .json(.init(
            userId: userId,
            score: score,
            force: force,
            disableEditMessage: disableEditMessage,
            chatId: chatId,
            messageId: messageId,
            inlineMessageId: inlineMessageId
        ))))
        switch output {
        case let .ok(ok):
            switch try ok.body.json.result {
            case let .Message(message): return message
            case .case2: return nil
            }
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "setGameScore", statusCode: statusCode, payload: payload
            )
        }
    }

    /// Use this method to get data for high score tables. Will return the score of the specified user
    /// and several of their neighbors in a game. Returns an Array of GameHighScore objects. This method
    /// will currently return scores for the target user, plus two of their closest neighbors on each
    /// side. Will also return the top three users if the user and their neighbors are not among them.
    /// Please note that this behavior is subject to change.
    @discardableResult
    func getGameHighScores(userId: Swift.Int64, chatId: Swift.Int64? = nil, messageId: Swift.Int64? = nil, inlineMessageId: Swift.String? = nil) async throws -> [Components.Schemas.GameHighScore] {
        try await self.pace(chatId: chatId.map(ChatId.id))
        let output = try await self.api.getGameHighScores(.init(body: .json(.init(
            userId: userId,
            chatId: chatId,
            messageId: messageId,
            inlineMessageId: inlineMessageId
        ))))
        switch output {
        case let .ok(ok):
            return try ok.body.json.result
        case let .undocumented(statusCode, payload):
            throw await TelegramAPIError.undocumented(
                operation: "getGameHighScores", statusCode: statusCode, payload: payload
            )
        }
    }
}
