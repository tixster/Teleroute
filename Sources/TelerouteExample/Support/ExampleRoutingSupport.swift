import Teleroute

// The example previously declared its own `ChatTypeGuard` and
// `AccessLogMiddleware`. Both now ship with Teleroute as
// `TelerouteChatTypeGuard` and `TelerouteAccessLogMiddleware`, so this file
// only needs to host any example-only helpers going forward.
